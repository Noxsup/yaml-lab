---
trigger:
  branches:
    include:
      - main

variables:
  appName: 'mywebapp'

stages:
  - stage: Build
    displayName: 'Build Stage'
    jobs:
      - job: Build
        displayName: 'Build Job'
        pool:
          vmImage: 'ubuntu-latest'
        steps:
          - checkout: self
          - script: echo "Building $(appName)"
